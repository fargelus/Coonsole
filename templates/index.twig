{% extends "content.twig" %}
{% block content %}
    {% if items is defined %}
        {% set json_params = items|json_encode(constant('JSON_UNESCAPED_UNICODE')) %}
        <div id="app">
            <App></App>
        </div>
    {% endif %}
{% endblock %}
